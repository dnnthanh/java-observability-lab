package dev.lab.provider;
import static org.assertj.core.api.Assertions.assertThat;
import org.junit.jupiter.api.Test;
class PaymentProviderApplicationTest {
  @Test void classIsPresent() { assertThat(PaymentProviderApplication.class.getSimpleName()).isNotBlank(); }
}
